import React from 'react'; // React를 import 해주세요.
import Editor from '../component/Editor';
const Home = () => {
    return (
        <div>
            <Editor />
        </div>
    );
};

export default Home;
